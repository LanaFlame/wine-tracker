$kind: http-request
name: Get All Wines
url: "{{baseUrl}}/wines"
method: GET
scripts:
  - type: afterResponse
    code: ' pm.test("Status is 200", () => { pm.response.to.have.status(200); });'
    language: text/javascript
order: 6000
